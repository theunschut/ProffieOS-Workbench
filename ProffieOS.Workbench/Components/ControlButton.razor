@inject SaberStateService State

<button class="wb-btn @(Active ? "active" : "")" type="button" @onclick="HandleClick">
    @if (!string.IsNullOrEmpty(Icon))
    {
        <MudIcon Icon="@Icon" Class="wb-btn-icon" />
    }
    <span class="wb-btn-label">@Label</span>
</button>
